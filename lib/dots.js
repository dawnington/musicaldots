// Variables: number of dots, radius of dots, duration of animation

const dots = (function(freq) {

  const

  const canvas = document.querySelector('.dots');
  const ctx = canvas.getContext('2d');
  const numDots =
})
